import React from 'react';
import ReactDOM from 'react-dom'; // É utilizado sempre que for renderizar os componentes (render)
import App from './components/App'

ReactDOM.render(
    <App />,
    document.getElementById("app") 
);